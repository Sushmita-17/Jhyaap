# Backend Setup TODO

- [ ] Create top-level backend folder `jhyaap-station-backend` (must NOT be inside `nightowl-liquors`).
- [ ] Create FastAPI app folder structure and placeholder `__init__.py` files.
- [ ] Add `requirements.txt`, `.env.example`, `.gitignore`, and `README.md`.
- [ ] Add minimal `app/main.py` with `FastAPI()` instance and `GET /health` endpoint.
- [ ] Add `setup_jhyaap_backend.sh` script that sets up venv, installs deps, and initializes git.
- [ ] After creation: user runs the script and then starts server with `uvicorn app.main:app --reload`.

